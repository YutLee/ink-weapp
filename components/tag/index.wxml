<view class="yos-class yos-tag {{status}} {{outline ? 'outline' : ''}} {{radius ? 'radius' : ''}}">
  <slot></slot>
  <icon wx:if="{{closeAble}}" type="{{closeIcon || 'guanbi1'}}" yos-class="close" bindtap="onClose"></icon>
</view>