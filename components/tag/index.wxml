<view class="ink-class ink-tag {{status}} {{outline ? 'outline' : ''}} {{radius ? 'radius' : ''}}">
  <slot></slot>
  <icon wx:if="{{closeAble}}" type="{{closeIcon || 'guanbi1'}}" ink-class="close" bindtap="onClose"></icon>
</view>