@import '../../theme.wxss';

.ink-list {
  background-color: var(--bgColor);
  padding: var(--spaceL);
  font-size: var(--textSize);
}

.ink-list.in-group {
  position: relative;
}

.ink-list.in-group:before {
  content: '';
  position: absolute;
  left: var(--spaceL);
  right: 0;
  top: 0;
  border-top: 1rpx solid var(--borderColor);
}

.ink-list.outline {
  border-top: 1rpx solid var(--borderColor);
  border-bottom: 1rpx solid var(--borderColor);
}

.ink-list.disabled, 
.ink-list.disabled .arrow, 
.ink-list.disabled .desc, 
.ink-list.disabled .value {
  color: var(--disabledColor);
}

.thumb {
  width: var(--sizeS);
  height: var(--sizeS);
  margin-right: var(--spaceM);
}

.icon {
  margin-right: var(--spaceM);
}

.desc, .value {
  color: var(--subTextColor);
}

.desc {
  font-size: var(--subTextSize);
}

.footer {
  text-align: right;
  padding: 0 var(--spaceM);
}

.arrow {
  color: var(--subTextColor);
}