<scroll-view 
  wx:if="{{scroll}}" 
  scroll-x="true" 
  scroll-y="{{direction === 'vertical'}}" 
  class="ink-class ink-tabs scroll"
  scroll-top="{{scrollTop}}"
  scroll-left="{{scrollLeft}}"
>
  <slot></slot>
</scroll-view>
<view class="ink-class ink-tabs flex" wx:else>
  <slot></slot>
</view>