@import "../../theme.wxss";

.ink-tabs {
  background-color: var(--bgColor);
}

.ink-tabs.flex {
  justify-content: space-around;
}

.ink-tabs.scroll {
  display: block;
  overflow: auto;
  white-space: nowrap;
}
