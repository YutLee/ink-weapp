@import "../../theme.wxss";

.ink-accordion .header {
  padding: var(--spaceL);
  font-size: var(--textSize);
}

.arrow {
  transition: transform .2s ease;
}

.arrow.open {
  transform: rotate(180deg);
}