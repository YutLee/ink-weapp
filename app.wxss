/**app.wxss**/
/* .container {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  padding: 200rpx 0;
  box-sizing: border-box;
}  */

page {
  /* padding: 10px; */
  background-color: var(--grayBgColor);
  box-sizing: border-box;
}

.title {
  color: var(--textTextColor);
  font-size: var(--textSize);
  padding: var(--spaceL);
  display: block;
}
