radio {
  display: block
}

.radio {
  display: flex !important;
}

.checked {
  color: var(--primaryColor);
}

.line {
  border-top: 1rpx solid var(--borderColor);
  margin: var(--spaceM) 0;
}